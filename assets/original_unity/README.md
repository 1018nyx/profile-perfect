# Original Unity Assets

This folder keeps the original `assets/` payload from:

`/Users/niuyaxue/Desktop/ProfilePerfect_xapk_unpacked/com.luwukmeliana.profileperfect.apk`

The complete original payload is preserved as:

`assets/original_unity/ProfilePerfect_apk_assets.zip`

Safe-to-import original resources are also expanded for Cocos inspection:

- `assets/original_unity/unity_bundles`: all 540 Unity Addressables `.bundle` files.
- `assets/original_unity/balancy`: original Balancy configuration JSON files.
- `assets/original_unity/unity_data`: original Unity data split files.
- `assets/original_unity/StreamingAssetsBalancy.zip`
- `assets/original_unity/UnityServicesProjectConfiguration.json`

The APK also contains web/ad `.js` and `.html` files. They stay inside `ProfilePerfect_apk_assets.zip` so Cocos Creator does not treat them as project scripts.

The generated import summary is:

`assets/resources/data/original-resource-manifest.json`
